---
import BaseLayout from "./BaseLayout.astro";

interface Props {
  frontmatter: {
    title?: string;
  };
}

const { frontmatter } = Astro.props;
---

<BaseLayout title={frontmatter.title}>
  <article class="markdown-body w-full p-8">
    <slot />
  </article>
</BaseLayout>
